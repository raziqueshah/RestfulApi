"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = __importDefault(require("path"));
const searchPackageJson = (directory) => {
    try {
        const contents = require(path_1.default.join(directory, '/package.json'));
        return contents.version;
    }
    catch (error) {
        // Look for the parent directory
        const parentDir = path_1.default.resolve(directory, '..');
        if (directory === parentDir) {
            // We've reached the root folder
            return '0.0.0';
        }
        return searchPackageJson(parentDir);
    }
};
const getVersion = () => {
    const currentDir = path_1.default.resolve(__dirname);
    return searchPackageJson(currentDir);
};
/**
 * Gets the version of this package.
 */
const version = getVersion();
exports.default = version;
//# sourceMappingURL=version.js.map